export function LogEntry(logEntry) {
  this.id = logEntry.id,
    this.sendDate = logEntry.sendDate,
    this.level = logEntry.level,
    this.clientId = logEntry.clientId,
    this.message = logEntry.message

}
